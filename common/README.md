# Common Utilities

This directory contains shared utilities used across all DE tools.

## Files

- `utils.py` - Common helper functions
- `templates/` - Reusable Docker Compose snippets

## Usage

These utilities are imported by the tool handlers in `_manage/` directory.

You typically don't need to modify these files directly.
